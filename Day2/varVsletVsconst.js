const browserName = 'Chrome';

function getBrowserName(){

    if (browserName === 'Chrome'){
     var browserName = 'local chrome';
     console.log(browserName);
    }
}
getBrowserName();
console.log(browserName);